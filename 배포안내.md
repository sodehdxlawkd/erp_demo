# 배포 안내 — 버셀(Vercel)로 인터넷에 올리기

> 🎓 이 앱은 교육용 가상 자료입니다. 올려도 실제 인사·급여 정보는 들어 있지 않습니다.

## 먼저 알아둘 것 — 왜 '배포용 사본'을 만드는가

버셀은 **한글(비ASCII) 파일명을 주소로 찾지 못해 404**를 냅니다. 실제로 시험해 확인했습니다
(퍼센트 인코딩·NFD·원문 주소 모두 404).

그렇다고 원본 파일명을 영문으로 바꾸면 **PRD §8의 '교육용 가상 자료 표시 4곳' 중 하나가 깨집니다.**
그래서 원본은 한글 그대로 두고, **배포할 때만** `vercel-build.mjs` 가 `dist/` 폴더에 영문 이름 사본을
만듭니다. 링크와 데이터 경로도 함께 바꿔 줍니다.

| 원본 (내 PC) | 배포본 (인터넷) |
|---|---|
| `인사총무ERP_교육용가상자료.html` | `edu-fictional-hr-erp.html` |
| `data/가상_인사총무.json` | `data/edu-fictional-hr-data.json` |
| `데이터안내.md` · `확인예시.md` · `레퍼런스.md` | `data-guide.md` · `check-example.md` · `reference.md` |

**내 PC에서 쓰는 방식은 하나도 바뀌지 않습니다.** 더블클릭도, `node 로컬서버.mjs` 도 그대로입니다.
`dist/` 는 자동 생성물이라 git에 올리지 않습니다.

## 연결하는 방법 (한 번만)

1. https://vercel.com 에 GitHub 계정으로 로그인합니다.
2. **Add New… → Project → Import Git Repository** 에서 `erp_demo` 를 고릅니다.
3. 설정 화면은 **아무것도 건드리지 말고** Deploy 를 누릅니다.
   `vercel.json` 에 빌드 방법(`node vercel-build.mjs`)과 출력 폴더(`dist`)가 이미 적혀 있습니다.
4. 1분 이내에 `https://<프로젝트이름>.vercel.app` 주소가 나옵니다.

이후에는 `git push` 할 때마다 자동으로 다시 배포됩니다.

## 주소

| 주소 | 화면 |
|---|---|
| `/` | 시작 화면 |
| `/app` | 앱 본체 (짧은 주소) |
| `/edu-fictional-hr-erp.html` | 앱 본체 (배포본 파일명) |
| `/PRD.md` · `/data-guide.md` 등 | 문서 |

## 배포하고 나면 확인할 것

- 앱 화면 왼쪽 아래에 **"data/… 에서 불러옴"** 이 뜨는지 (데이터 파일을 실제로 읽는다는 뜻)
- 문서 링크가 내려받기 없이 화면에 보이는지
- **AI 연결 시험 화면은 인터넷 배포본에서 작동하지 않습니다.** API 키를 브라우저에 넣지 않기
  위해 내 PC의 `로컬서버.mjs` 가 대신 호출하는 구조이기 때문입니다 (의도된 동작입니다).

## 로컬 실행은 그대로입니다

- 앱 파일 **더블클릭** — 내장 데이터로 실행 (설정 불필요)
- `node 로컬서버.mjs` — 데이터 파일을 실제로 읽고, AI 연결 시험도 이때만 작동
