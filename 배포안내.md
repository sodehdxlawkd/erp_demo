# 배포 안내 — 버셀(Vercel)로 인터넷에 올리기

> 🎓 이 앱은 교육용 가상 자료입니다. 올려도 실제 인사·급여 정보는 들어 있지 않습니다.

이 폴더는 **빌드 도구가 없는 정적 HTML**이라, 버셀에 연결하면 설정 없이 바로 배포됩니다.

## 연결하는 방법 (한 번만)

1. https://vercel.com 에 GitHub 계정으로 로그인합니다.
2. **Add New… → Project → Import Git Repository** 에서 이 저장소를 고릅니다.
3. 설정 화면은 **아무것도 건드리지 말고** Deploy 를 누릅니다.
   - Framework Preset: `Other` (자동으로 잡힙니다)
   - Build Command / Output Directory: **비워 둡니다**
4. 1분 이내에 `https://<프로젝트이름>.vercel.app` 주소가 나옵니다.

이후에는 `git push` 할 때마다 자동으로 다시 배포됩니다.

## 이 폴더에 들어 있는 배포 설정

| 파일 | 하는 일 |
|---|---|
| `vercel.json` | `.md` 문서를 내려받기 대신 화면에 바로 띄우고, 데이터 JSON은 항상 최신으로 받게 합니다. 짧은 주소 `/app` 으로도 앱이 열립니다 |
| `.vercelignore` | `미사용/`·`로컬서버.mjs`·`CLAUDE.md` 는 인터넷에 올리지 않습니다 |

## 주소

| 주소 | 화면 |
|---|---|
| `/` | 시작 화면 |
| `/app` | 앱 본체 (짧은 주소) |
| `/인사총무ERP_교육용가상자료.html` | 앱 본체 (원래 파일명) |

## 배포하고 나면 꼭 확인할 것

앱 파일명이 한글이라, 배포 후 **앱 화면이 열리는지** 한 번 눌러 확인해 주세요.
혹시 원래 파일명 주소가 열리지 않으면 짧은 주소 `/app` 을 쓰면 됩니다.
(파일명은 PRD §8의 '교육용 가상 자료 표시 4곳' 중 하나라서 영문으로 바꾸지 않습니다.)

앱 화면 왼쪽 아래에 **"data/가상_인사총무.json 에서 불러옴"** 이라고 떠야 정상입니다.
버셀은 웹서버이므로, 더블클릭으로 열 때와 달리 데이터 파일을 실제로 읽어 옵니다.

## 로컬 실행은 그대로입니다

- 앱 파일 **더블클릭** — 내장 데이터로 실행 (설정 불필요)
- `node 로컬서버.mjs` — `data/가상_인사총무.json` 을 실제로 읽어 실행
