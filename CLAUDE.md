# CLAUDE.md

이 폴더에서 작업할 때 Claude Code가 참고하는 안내서입니다.

## 이 폴더는 무엇인가

**교육용 가상 인사총무 ERP 실습 앱 하나**를 만드는 폴더입니다. 가상 회사 「한빛상사(주)」의
창작된 인사·휴가·비품·급여 자료로, 비개발자에게 "인사총무 데이터가 어떻게 연결되고
어떻게 계산되는가"를 가르치기 위한 자료입니다. 실존 회사·개인·급여와 무관합니다.

package.json·빌드도구·린터·테스트러너가 없습니다. 만들 것도 없습니다.

## 파일 구성

| 파일 | 역할 |
|---|---|
| `인사총무ERP_교육용가상자료.html` | **앱 본체.** 단일 HTML 1개(인라인 CSS·SVG·JS). 더블클릭으로 실행 |
| `index.html` | 시작 화면. 앱 링크 + 문서 링크 + 지금 file:// 인지 http:// 인지 안내 |
| `로컬서버.mjs` | 선택용 로컬 서버(`node 로컬서버.mjs` → http://127.0.0.1:8123). 외부 패키지 없음 |
| `data/가상_인사총무.json` | 앱 데이터의 사본(항목명 한글). 서버로 열었을 때만 실제로 읽힘 |
| `PRD.md` | **정본.** 목적·데이터명세·계산규칙·동작규칙·확인기준(§9 체크리스트) |
| `DESIGN.md` | 겉모습 기준(색·글꼴·여백·버튼 토큰). 기능·데이터 규칙은 건드리지 않음 |
| `데이터안내.md` | 직원·휴가·급여 표를 왜 나누고 사번으로 어떻게 잇는지 |
| `확인예시.md` | 직원 한 명(강도현)으로 따라가는 입력값·예상결과 |
| `레퍼런스.md` | flex·Frappe HR·Odoo 공식 문서에서 가져온 설계 아이디어 |
| `미사용/` | **이 앱과 무관한 옛 파일들. 읽지도 고치지도 말 것** (DART 조회 앱, Gemini 예제 등) |

**앱 파일명을 `index.html`로 바꾸지 않습니다.** 파일명 자체가 PRD §8의 "교육용 가상 자료
표시 4곳" 중 하나입니다.

## 앱 구조 (`인사총무ERP_교육용가상자료.html`)

- **데이터**: 파일 안에 `DATA_EMP`(직원 12) · `DATA_LEAVE_INIT`(휴가신청 24) ·
  `DATA_ITEM_INIT`(비품 10) · `DATA_SUPPLY_INIT`(비품신청 18) · `DATA_PAY`(급여 12)가
  하드코딩되어 있습니다. http://로 열면 `loadExternalData()`가
  `data/가상_인사총무.json`을 fetch 해 덮어쓰고, 실패하면 조용히 내장 데이터로 돌아갑니다
  (`DATA_SOURCE`에 '내장'/'파일' 표시). **둘은 항상 같은 내용이어야 합니다 — 한쪽만 고치지 마세요.**
- **상태**: 모듈 수준 `role`(`'staff'`|`'admin'`) 하나로 직원/담당자 화면을 전환합니다.
  `render()`가 전체를 다시 그리는 방식이라, 부분 DOM 수정이 아니라 이 재렌더 방식을 그대로 확장합니다.
- **저장 안 함**: 새로고침하면 초기 상태로 돌아갑니다(localStorage 미사용). 「↺ 데이터 초기화」
  버튼도 있습니다. 수업 때 항상 같은 출발점이어야 하기 때문입니다 (PRD §6-6).
- **차트**: 직접 만든 인라인 SVG 막대그래프(`barChart`). 차트 라이브러리·canvas 없음.

## 반드시 지킬 규칙

- **더블클릭 실행을 깨뜨리지 않습니다.** 로컬 서버는 어디까지나 선택입니다 (PRD §6-8).
- **외부 CDN·라이브러리·빌드도구·npm 패키지를 쓰지 않습니다.** 서버도 Node 기본 기능만.
- **빈 값을 임의로 채우지 않습니다.** 대기 상태의 처리자·처리일은 `null`이고 화면엔 `—`입니다 (PRD §6-4).
- **"지금 얼마인가"는 계산하고, "그때 얼마였나"는 저장합니다.** 잔여연차는 매번 계산하고,
  급여 지급액은 저장된 과거 기록이라 조회 전용입니다 (PRD §6-7).
- **연차 계산**: 승인된 연차·반차만 차감(반차 0.5일), 병가는 어떤 상태에서도 차감 없음 (PRD §5-1).
- **교육용 가상 자료 표시 4곳**(파일명·상단 고정 배너·표 아래 각주·파일 첫 줄 주석, 추가로
  JSON의 `_안내`)을 지웁니다/줄이지 않습니다 (PRD §8).
- 동작을 바꿨으면 **PRD §9 확인 기준의 숫자로 대조**합니다 (예: 전사 부여 199일 / 사용 38.0일 /
  잔여 161.0일, 부서 소진율 경영지원 18.6% · 영업 22.0% · 물류 15.6% · 개발 20.0%).

## 사용자와 대화하는 방식

- 사용자는 개발자가 아닌 실무 담당자입니다. 코드 구현 용어가 아니라 **업무·데이터 용어**로
  설명합니다(한 줄이 무엇을 뜻하는지, 이 숫자가 무엇을 재는지).
- 집계하기 전에 **한 줄이 무엇을 뜻하는지 먼저 확인**합니다.
- 빠진 데이터를 어떻게 다뤘는지 항상 말로 밝힙니다.
